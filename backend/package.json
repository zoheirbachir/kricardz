{
  "name": "kricar-backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "seed": "node seed.js",
    "build": "echo \"Nothing to build on the server — the frontend is pre-bundled in public/.\"",
    "build:frontend": "node -e \"process.exit(require('fs').existsSync('../frontend')?0:1)\" && npm --prefix ../frontend install && npm --prefix ../frontend run build || echo \"[build:frontend] ../frontend not present — using the pre-bundled public/ instead.\"",
    "bundle:frontend": "node scripts/bundle-frontend.js"
  },
  "engines": {
    "node": ">=22.5"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "helmet": "^7.2.0",
    "jsonwebtoken": "^9.0.2",
    "multer": "^2.0.0",
    "nodemailer": "^9.0.3",
    "socket.io": "^4.8.3",
    "uuid": "^11.0.0"
  }
}
