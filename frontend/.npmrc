# Help managed build environments (e.g. Hostinger) locate node + run binaries
scripts-prepend-node-path=true
# Don't fail the install if the host's Node is slightly off from "engines"
engine-strict=false
